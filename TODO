Implement letrec* and use it in the expansion of internal definitions.

Protect symbols used by the core from effects of redefinition
(e.g. redefining '+' in r4rstest.wisp breaks the reader).

Install the repl.

Exceptions need some sort of type field; add accessors.

Macros should be shadowed by let and lambda bindings.

Get byte code compiler working properly.

